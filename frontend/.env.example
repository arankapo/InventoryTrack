# Base URL of the InvenTrack backend API.
# Local dev: leave unset — vite.config.js proxies /api to http://localhost:4000
# Production (Vercel): set to your deployed Railway backend URL, e.g.
# VITE_API_URL="https://inventrack-backend.up.railway.app/api"
VITE_API_URL=
